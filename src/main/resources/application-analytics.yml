spring:
  ai:
    mcp:
      client:
        enabled: true
        type: SYNC
        stdio:
          connections:
            smarthub:
              command: java
              # build jar truoc bang gradlew bootJar roi moi chay profile analytics
              args:
                - "-jar"
                - "build/libs/smarthub-0.0.1-SNAPSHOT.jar"
                - "--spring.profiles.active=mcp"
